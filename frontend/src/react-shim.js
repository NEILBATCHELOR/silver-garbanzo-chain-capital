// react-shim.js
import * as React from 'react';

export const {
  useState,
  useEffect,
  useContext,
  useReducer,
  useCallback,
  useMemo,
  useRef,
  useLayoutEffect,
  createContext,
  forwardRef,
  memo,
  createElement,
  Fragment,
  isValidElement,
  Children,
  Component
} = React;

export default React;
